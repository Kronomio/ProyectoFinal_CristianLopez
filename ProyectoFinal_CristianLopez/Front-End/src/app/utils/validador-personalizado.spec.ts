import { ValidadorPersonalizado } from './validador-personalizado';

describe('ValidadorPersonalizado', () => {
  it('should create an instance', () => {
    expect(new ValidadorPersonalizado()).toBeTruthy();
  });
});
